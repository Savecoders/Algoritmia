
const myProfile = (name, lastname, nickname) => {
  const completeName = name + " " + lastname
  return `Mi nombre es ${completeName} pero prefiero que me digan ${nickname}  `
}

const realMoney = (savings, debts) => savings - debts


realMoney(1000, 300))
